# Project Objective: publish & maintain the Sanskrit→Russian glossary (surface · lemma · root)

Standalone home for the three-layer Sa→Ru glossary built from the 1.09M-token aligned corpus
[`corpus_lexicon.jsonl`](https://github.com/gasyoun/SanskritLexicography/blob/master/RussianTranslation/src/corpus_lexicon.jsonl).
Data + [live searchable site](https://gasyoun.github.io/SanskritRussian/) live here; the
**generation pipeline** lives in
[`SanskritLexicography/RussianTranslation/src`](https://github.com/gasyoun/SanskritLexicography/tree/master/RussianTranslation/src)
(`build_dcs_maps.py` → `build_surface_glossary.py` → `build_rollup_glossaries.py`, with
`build_vidyut_fallback.py`).

## ➡️ Next Steps (Queue)
- **Marker-residual recovery** — 1,389 unresolved forms carry `+`/`-` marks whose rightmost
  element is itself inflected; a light morphological pass could reclaim some.
- **Search UX** — add Devanagari/IAST input (currently SLP1 or Russian only); optional
  surface-layer search (loads the 41 MB TSV — lazy/streamed).
- **Provenance surfacing** — show `source` (dcs/vidyut/marker) and register breakdown per entry
  in `index.html`.
- **Homograph handling** — the pipeline now captures the FULL alternate trail
  ([`ambiguity_homographs.tsv`](https://github.com/gasyoun/SanskritRussian/blob/main/ambiguity_homographs.tsv)
  + `vidyut_ambiguity.tsv`, H1349 W1.2/W1.3); still TODO is exposing those alternates in
  `index.html` instead of silently showing only the dominant lemma.
- **Wave-2 per-tier precision** — turn the new coverage≠accuracy caveat into a measured number
  (DCS / Vidyut / marker × frequency); the highest-value follow-up (H1349 wave 2, judgment-gated).
- **Republish (human-gated, D8)** the H1349-fixed data, then update the root count 2,021 → ~1,853
  and the coverage/typology tables here in the same pass.

## 🚧 Current Work-In-Progress (WIP)
- None — v1.1.1 docs shipped (H1349 wave-1 caveat + metadoc + √gam reconciliation).

## 🧠 Dev Notes & Hypotheses
- **Windows case-collision (fixed).** SLP1 is case-significant (`a`≠`A`) but Windows' FS is
  case-insensitive → per-letter shard files `a`/`A` collided & truncated, losing 23k records
  until bucket names were **case-folded to upper**. Any future per-key file sharding must fold.
- **Pages slot is single-per-repo.** This repo exists *because* SanskritLexicography's Pages
  serves the PWG article dashboard (`gh-pages`); don't try to co-host — keep the glossary here.
- **Regeneration is cross-repo.** Editing the pipeline happens in SanskritLexicography; the
  regenerated data is copied here. Keep the two in sync on any schema change.

## ✅ Completed (Recent only)
- **v1.1.1 (2026-07-20)** — H1349 wave-1 docs: coverage≠accuracy caveat on README + `index.html`,
  `README.meta.md` companion metadoc, README made canonical (D11), `√gam` numbers reconciled to the
  current-data regen. Published data unchanged (republish is human-gated, D8). Opus 4.8 (`claude-opus-4-8`).
- **v1.0.1 (2026-07-01)** — `INTERLINKS.md` documenting upstream/downstream repo links;
  registered the derived asset in [`Uprava/PROJECT_INTERLINKS.md`](https://github.com/gasyoun/Uprava/blob/main/PROJECT_INTERLINKS.md).
- **v1.0.0 (2026-07-01)** — three layers (surface 190,838 · lemma 40,370 · root 2,021), DCS
  form→lemma + Vidyut kosha + morpheme-marker join (**87.1 % token coverage**), documented
  failure typology, per-letter surface JSONL split (26 parts, case-folded), client-side search
  `index.html`, `.nojekyll` static serving. Live at <https://gasyoun.github.io/SanskritRussian/>.
- De-duplicated out of SanskritLexicography HEAD (that repo keeps only the pipeline).

_Dr. Mārcis Gasūns_
